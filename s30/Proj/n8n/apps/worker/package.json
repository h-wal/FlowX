{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @repo/worker-core build && nodemon",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/worker-core": "workspace:*",
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "nodemon": "^3.1.10",
    "typescript": "^5.9.2"
  }
}
